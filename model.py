import numpy as np


def predict(image):
    """Simple thresholding segmentation model.

    Args:
        image: numpy array, uint8. Shape (H, W, 3) RGB lub (H, W) grayscale.

    Returns:
        Binary mask as numpy array of shape (H, W), uint8 with values 0 or 255.
    """
    pass
